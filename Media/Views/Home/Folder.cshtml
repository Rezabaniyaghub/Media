@model Folder


<dive class="row mb-5">
    <dive class="col-2">
        <a href="/home/AddNewFolder?parentId=@(Model.Id)" class=" btn btn-primary">
            <i class="fas fa-folder-plus"></i>
        </a>
    </dive>
</dive>
@if (Model.Childs != null && Model.Childs.Count() > 0)
{
    <h2 class="text text-primary col-12">@Model.Title</h2>
    <span class="text text-sm col-12">@Model.Description</span>
    <hr />
    <div class="row">
        @foreach (var item in Model.Childs)
        {
            <div class="col-2">
                <a asp-action="folder" asp-route-folderId="@item.Id">
                    <i class="fas fa-folder-open" style="font-size:140px;color:#f2d06c"></i>
                </a>
                <br>
                <span class="text">@item.Title</span>
            </div>
        }
    </div>
}

@if (Model.Files != null && Model.Files.Count() > 0)
{
    <h2 class="text text-primary">Files</h2>
    <hr />
    <div class="row">
        @foreach (var item in Model.Files)
        {
            <div class="col-2">
                <i class="fas fa-file" style="font-size:140px;color:#f2d06c"></i>
                <br>
                <span class="text">@item.Title</span>
            </div>
        }
    </div>
}